enum TextFieldType {
  text,
  password,
  number,
  phonenNumber
}

enum AlertType {
  success,
  error,
  dialog
}

enum TransactionType {
  sent,
  received
}

enum FormType {
  add,
  create,
  update
}

enum TrashType {
  agents,
  users,
  transactions
}

enum TransactionStatus {
  completed,
  pending,
  canceled
}

enum OverviewType {
  inline,
  floating
}

enum Currency {
  etb,
  dollar,
  pound,
  euro,
  japaneseYen,
  chineseYen
}

enum Banks {
  cbe,
  coop,
  abyssinia,
  nib,
  awash,
  anbessa, 
  wegagen
}